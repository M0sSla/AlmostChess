package main.model.entities;

import main.model.Piece;

public class Imp extends Piece {

    public Imp(Position position, PieceColor pieceColor, Tier pieceType, Double speed) {
        super(position, pieceColor, pieceType, speed);
    }
}
